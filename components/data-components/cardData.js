const nativeSizes = [26, 30, 40];
const nativeTypes = [
    {
        'id': 0,
        'type': 'тонкое'
    },

    {
        'id': 1,
        'type': 'традиционное'
    }
]

let obj = {}

const buttonsCardStyles =
  "rounded-[5px] py-[10px] bg-inherit text-[14px] leading-[17.05px] font-extrabold text-[#2C2C2C] text-center hover:bg-white";

export { nativeSizes, buttonsCardStyles, nativeTypes };
